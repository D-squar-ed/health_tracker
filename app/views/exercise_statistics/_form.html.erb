<%= form_for(@exercise_statistic) do |f| %>
  <% if @exercise_statistic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exercise_statistic.errors.count, "error") %> prohibited this exercise_statistic from being saved:</h2>

      <ul>
      <% @exercise_statistic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :exercise_type_id, "Choose Exercise Type" %><br>
    <%= f.collection_select(:exercise_type_id, ExerciseType.all, :id, :name, include_blank: true) %>
  </div>

  <div class="field">
    <%= f.label :duration %><br>
    <%= f.number_field :duration %>
  </div>

  <div class="field">
    <%= f.label :nomnoms_lost %><br>
    <%= f.number_field :nomnoms_lost %>
  </div>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>



  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
