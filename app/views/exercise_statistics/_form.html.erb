<%= form_for(@exercise_statistic) do |f| %>
  <% if @exercise_statistic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exercise_statistic.errors.count, "error") %> prohibited this exercise_statistic from being saved:</h2>

      <ul>
      <% @exercise_statistic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :exercise_type_id %><br>
    <%= f.select :exercise_type_id, ([["Swimming", 1], ["Sprints", 2],
    ["Jogging", 3], ["Weight-Lifting", 4], ["Tai Chi", 5],
    ["Rock-Climbing", 6]]) %>
  </div>

  <div class="field">
    <%= f.label :duration %><br>
    <%= f.number_field :duration %>
  </div>

  <div class="field">
  <%= f.label :nomnoms_lost %><br>
  <%= f.number_field :nomnoms_lost %>
</div>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>



  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
